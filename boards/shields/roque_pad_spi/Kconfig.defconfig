if SHIELD_ROQUE_PAD_SPI

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Roque Pad SPI"

config ZMK_POINTING
    default y

endif

# --- Pinctrl Kconfig for part "unibody" ---
if BOARD_NICE_NANO_V2 && SHIELD_ROQUE_PAD_SPI

config SPI
    default y

endif
