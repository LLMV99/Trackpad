if SHIELD_TRACKPAD

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Trackpad"

config ZMK_POINTING
    default y

endif

# --- Pinctrl Kconfig for part "unibody" ---
if BOARD_NICE_NANO_V2 && SHIELD_TRACKPAD

config SPI
    default y

endif
